C语言中与条件编译相关的预处理指令主要包括 #if、#ifdef、#ifndef、#elif、#else 和 #endif。这些指令在程序编译前的预处理阶段执行，根据条件决定某些代码是否参与编译。

其中，#if 用于根据表达式的真假进行条件编译，#ifdef 用于判断某个宏是否已经定义，#ifndef 用于判断某个宏是否未定义，#elif 和 #else 用于多分支条件选择，而 #endif 用于结束条件编译块。

条件编译常用于调试代码控制、防止头文件重复包含、适配不同操作系统或硬件平台以及实现不同版本功能等场景。通过条件编译可以提高代码的可维护性和可移植性。