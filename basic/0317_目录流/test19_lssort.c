#include <dirent.h>
#include <my_header.h>

int main(int argc, char *argv[]){

    // 1.通过目录流。 获取文件名。 是单个的，无法直接排序。
    // 需要用一个列表将其存储下来。 搞一个数组。将信息存储下来。 
    // dirent  
    
    // qsort : 
    //

    return 0;
}

